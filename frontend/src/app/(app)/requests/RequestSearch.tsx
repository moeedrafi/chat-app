"use client";

export const RequestSearch = () => {
  return (
    <div>
      <input
        onChange={() => {}}
        placeholder="Search for users"
        className="w-full text-sm bg-light px-3 py-2 rounded-xl ring-1 ring-color focus-visible:ring-2 outline-none disabled:cursor-not-allowed disabled:opacity-50"
      />
    </div>
  );
};
