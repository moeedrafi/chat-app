export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum MessageStatus {
  SENT = 'sent',
  READ = 'read',
}

export enum FriendRequestStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
}
